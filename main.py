# feeds_db  > will call news, filter and adds to db if they haven't been.
# ai_preperation.py > must run after feeds_db. It fetches data including link and title. Gets main text from link and returns dict as {"title": title, "link": link, "sumamry": summary} This data is then ready to send to ai for translation.